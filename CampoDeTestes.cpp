#include <iostream>
#include <vector>
using namespace std;

vector<int>* controle; // É assim que se deve instânciar vectors/etc GLOBAIS

int main(){

   /*  controle = new vector<int> [3];

    cout << controle->size() << endl;

    //Fill
    for(int i = 0 ; i < 3; i++){
        controle[i].push_back(8);
        controle[i].push_back(8);
        controle[i].push_back(8);
        controle[i].push_back(8);
    }

    //Print
    for(int i = 0 ; i <  3; i++){
        for(int x : controle[i]){
            cout << x << ' ';
        }
        cout << endl;
    } */

    return 0;
}